import React, {FC} from 'react';
import '../../../styles/_header.scss'

export const Header: FC = () => {
    return (
        <div className='header'>
            Это header!
        </div>
    );
};